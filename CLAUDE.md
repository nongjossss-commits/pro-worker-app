# CLAUDE.md — Pro-Worker v2

## บริบทโปรเจกต์

- โปรแกรมนี้อยู่ในสถานะ **Production (ใช้งานจริง)** — มีผู้ใช้งานอยู่
- โค้ดเดิมถูกสร้างโดย AI อีกค่าย อาจมีรูปแบบที่ต่างออกไป — **ห้ามแก้ให้เป็นสไตล์ใหม่โดยไม่จำเป็น**
- เป้าหมายปัจจุบัน: **ซ่อมและเพิ่มฟีเจอร์ทีละจุด** ไม่ใช่ rewrite หรือ refactor ทั้งระบบ
- การ refactor ครั้งใหญ่จะทำ **เมื่อได้รับอนุญาตชัดเจนเท่านั้น**

---

## กฎหลัก — ห้ามทำโดยไม่ได้รับอนุญาต

### UI / หน้าตาโปรแกรม
- ห้ามเปลี่ยน layout, สี, font, spacing, หรือโครงสร้าง HTML ที่มีอยู่
- ห้ามเปลี่ยนชื่อ label, ข้อความ, หรือ placeholder ที่ผู้ใช้มองเห็น
- ห้ามเพิ่มหรือลบ column ในตารางข้อมูล
- ห้ามเปลี่ยนลำดับหรือตำแหน่งของปุ่มและเมนู

### Database
- ห้ามแก้ไข migration ที่ run ไปแล้ว — ให้สร้าง migration ใหม่เสมอ
- ห้ามเปลี่ยนชื่อ column หรือ table โดยไม่แจ้งผลกระทบก่อน
- ห้าม seed หรือลบข้อมูลใน production

### Logic / โค้ด
- ห้ามเปลี่ยน business logic หรือ flow การทำงานที่มีอยู่ เว้นแต่ถูกแจ้งว่ามีปัญหา
- ห้าม refactor โค้ดที่ทำงานได้ปกติ เพียงเพราะต้องการให้สวยงามหรือสมบูรณ์กว่าเดิม
- ห้ามเปลี่ยนชื่อ function, variable, หรือ class ที่มีอยู่โดยไม่จำเป็น
- ห้ามเพิ่ม dependency หรือ package ใหม่โดยไม่แจ้งก่อน

---

## แนวทางการทำงาน

1. **อ่านไฟล์ก่อนแก้เสมอ** — ห้ามเดาหรือสมมติโครงสร้างของไฟล์
2. **แก้เฉพาะจุดที่ถูกขอ** — ไม่แตะโค้ดส่วนอื่นที่ไม่เกี่ยวข้อง
3. **แจ้งรายชื่อไฟล์ที่จะแก้ไขก่อนเสมอ** และรอการยืนยัน ถ้างานมีผลกระทบกว้าง
4. **ถามก่อนทำ** ถ้าไม่แน่ใจว่าการแก้จะกระทบส่วนอื่น
5. **รายงานสิ่งที่พบ** เช่น bug หรือจุดที่ควรปรับปรุง แต่ไม่แก้โดยพลการ
6. ถ้าพบโค้ดที่ทำงานได้แต่ไม่ ideal — **บันทึกไว้รายงาน** อย่าแก้ทันที

---

## ระดับการแก้ไขที่อนุญาต (ปัจจุบัน)

| ระดับ | คำอธิบาย | สถานะ |
|-------|----------|--------|
| Bug Fix | แก้ไขสิ่งที่ทำงานผิดพลาด | อนุญาต |
| Feature Addition | เพิ่มฟีเจอร์ใหม่ตามที่ขอ | อนุญาต (เฉพาะที่ขอ) |
| Minor Cleanup | ปรับโค้ดเล็กน้อยระหว่างแก้ bug | อนุญาตในขอบเขตแคบ |
| Refactor | เปลี่ยนโครงสร้างโค้ด | ต้องขออนุญาตก่อน |
| Full Rewrite | เขียนใหม่ทั้งระบบหรือ module | ยังไม่อนุญาต |
